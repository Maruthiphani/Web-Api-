@model ConsumeWebAPI.Models.ProductViewModel

@{
    ViewBag.Title = Model != null ? "Edit" : "Create";
}

<h1>@ViewBag.Title</h1>
<hr />

<h4>ProductViewModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="@(Model != null && Model.Id != 0 ? "Edit" : "Create")" method="@(Model != null && Model.Id != 0 ? "Post" : "Post")">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @* <div class="form-group">
            <label asp-for="Id" class="control-label"></label>
            <input asp-for="Id" class="form-control" />
            <span asp-validation-for="Id" class="text-danger"></span>
            </div>*@
            @Html.HiddenFor(Model => Model.Id)

            <div class="form-group">
                <label asp-for="ProductName" class="control-label"></label>
                <input asp-for="ProductName" class="form-control" />
                <span asp-validation-for="ProductName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Price" class="control-label"></label>
                <input asp-for="Price" class="form-control" />
                <span asp-validation-for="Price" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Quantity" class="control-label"></label>
                <input asp-for="Quantity" class="form-control" />
                <span asp-validation-for="Quantity" class="text-danger"></span>
            </div>
            @*<div class="form-group">
            <input type="submit"  value="Create" class="btn btn-primary" />
            </div>*@
            <div class="form-group">
                <input type="submit" value="@((Model?.Id != null && Model.Id != 0) ? "Edit" : "Create")" class="btn btn-primary" />
            </div>

        </form>
    </div>
</div>

@*<div>
    <a asp-action="Index">Back to List</a>
</div>*@

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}

@*<form asp-action="@(Model != null && Model.Id != 0 ? "Edit" : "Create")" method="@(Model != null && Model.Id != 0 ? "Post" : "Get")">
    <!-- Form fields and submit button -->
    <input type="submit" value="@((Model != null && Model.Id != 0) ? "Edit" : "Create")" class="btn btn-primary" />
</form>*@
